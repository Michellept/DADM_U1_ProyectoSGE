<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vs.layour.dadm_u1_proyectosge" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DADM_U1_ProyectoSGE" >
        <activity android:name=".ReticulaActivity" >
        </activity>
        <activity android:name=".KardexActivity" />
        <activity android:name=".HorarioActivity" />
        <activity android:name=".EvaluacionActivity" />
        <activity android:name=".DpersonalesActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LogInActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>